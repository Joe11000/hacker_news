<div class="container">
  <div class="controls">
    <h1><%= @post.try(:title) %></h1>
    <br>
    <p><%= @post.try(:comment) %></p>
    <br>
    <p>Comments:</p>
    <ul>
    <% @post.comments.each do |comment| %>
      <li><%= comment.body %></a></li>
    <% end %>
    </ul> 
  </div>
</div>
<a href="/">Back</a>   

