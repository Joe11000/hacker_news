<div class="container">
  <div class="controls">
    <h1><%= @post.try(:title) %></h1>
    <br>
    <p><%= @post.try(:comment) %></p>
    <br>
    <p>Comments:</p>
    <ul>
    <% @post.comments.each do |comment| %>

      <form method="upvote" action="/upvote/post/<%= comment.id %>"> 
        <input type="submit" value="&#x25B2;">
      </form>
      <li><%= comment.body %></a>
         <a href="/profile/<%=comment.user.id%>"><%=comment.user.name %>
         </li>
    <% end %>
    </ul> 
  </div>
</div>
<a href="/">Back</a>   

